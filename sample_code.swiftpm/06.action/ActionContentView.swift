//
//  ContentView.swift
//  wechat

import SwiftUI

struct ActionContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
        }
        .padding()
    }
}

struct ActionContentView_Previews: PreviewProvider {
    static var previews: some View {
        ActionContentView()
    }
}
