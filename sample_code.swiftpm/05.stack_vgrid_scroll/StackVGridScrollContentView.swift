//
//  ContentView.swift
//  wechat

import SwiftUI

struct StackVGridScrollContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
        }
        .padding()
    }
}

struct StackVGridScrollContentView_Previews: PreviewProvider {
    static var previews: some View {
        StackVGridScrollContentView()
    }
}
